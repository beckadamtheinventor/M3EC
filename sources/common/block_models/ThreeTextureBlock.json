{
    "parent": "block/cube_bottom_top",
    "textures": {
        "bottom": "${mod.mcpath}:blocks/${texture_bottom}",
		"top": "${mod.mcpath}:blocks/${texture_top}",
        "side": "${mod.mcpath}:blocks/${texture_side}"
    }
}


public class ${contentid} extends ${tooltype} {
	public ${tooltype}(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
		super(material, attackDamage, attackSpeed, settings);
	}
}

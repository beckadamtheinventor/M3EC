package ${mod.package}.recipetype;

import com.google.gson.JsonObject;

class ${mod.recipetype.${%v}.class}JsonFormat {
	JsonObject inputA;
	JsonObject inputB;
	String outputItem;
	int outputAmount;
}